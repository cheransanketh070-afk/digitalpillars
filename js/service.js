(()=>{const reduce=matchMedia('(prefers-reduced-motion: reduce)').matches;if(matchMedia('(pointer:fine)').matches&&!reduce){document.querySelectorAll('[data-tilt]').forEach(el=>{el.addEventListener('pointermove',e=>{const r=el.getBoundingClientRect(),x=e.clientX/r.width-r.left/r.width-.5,y=e.clientY/r.height-r.top/r.height-.5;el.style.transform=`perspective(900px) rotateX(${(-y*5).toFixed(2)}deg) rotateY(${(x*6).toFixed(2)}deg) translateZ(7px)`});el.addEventListener('pointerleave',()=>el.style.transform='')})}const modal=document.getElementById('briefModal');document.querySelectorAll('[data-brief]').forEach(b=>b.onclick=()=>modal.classList.add('is-open'));document.querySelectorAll('[data-close]').forEach(b=>b.onclick=()=>modal.classList.remove('is-open'));document.getElementById('briefForm').onsubmit=e=>{e.preventDefault();const f=new FormData(e.currentTarget),sub=encodeURIComponent(`Digital Pillars enquiry — ${f.get('company')||f.get('name')}`),body=encodeURIComponent(`Service: ${window.__SERVICE__}\nName: ${f.get('name')}\nEmail: ${f.get('email')}\nCompany: ${f.get('company')}\n\nProject:\n${f.get('message')}`);location.href=`mailto:hello@digitalpillars.studio?subject=${sub}&body=${body}`}})();